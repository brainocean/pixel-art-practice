# AGENTS.md — 像素画自学仓库

## 项目概述

个人像素画自学仓库，目标是从零基础到能独立完成游戏所需资产。
主工具：**Aseprite**。练习节奏弹性，按完成顺序推进，不绑定日历日期。

---

## 文件结构

```
pixel-art-practice/
├── pixel-art-tutorial.md   # 教程参考（只增不删正文）
├── exercise-plan.md        # 12 周练习计划模板（只增不删正文）
├── progress.md             # 进度记录（agent 可自由修改）
├── week-01/                # 第 1 周练习作品
├── week-02/
└── …
```

### 文件权限说明

| 文件 | 读 | 写 |
|------|----|----|
| `pixel-art-tutorial.md` | ✅ | 只允许在末尾追加新内容，不得删改已有正文 |
| `exercise-plan.md` | ✅ | 只允许在末尾追加新内容，不得删改已有正文 |
| `progress.md` | ✅ | 自由修改 |
| `week-XX/*.png` / `*.aseprite` | ✅（视觉分析） | ✅ |

---

## Agent 职责（优先级从高到低）

1. **推进练习进度**：读取 `progress.md` 确认当前位置，给出下一步任务，并在任务完成后更新 `progress.md`。
2. **生成针对性练习**：学完某节理论后，根据当前技术水平生成额外练习题或变体任务。
3. **补充教程内容**：查阅 Lospec、saint11、Slynyrd 等一手资源，将新知识追加到 `pixel-art-tutorial.md`。

---

## 进度追踪（progress.md）

### 初始化

若 `progress.md` 不存在，agent 应按以下格式创建：

```markdown
# 像素画练习进度

## 当前位置
- 阶段：第 1–2 周（基础技术）
- 下一任务：第 1 周 第 1 天

## 完成记录

| 完成日期 | 周次 | 天次 | 任务简述 | 备注 |
|----------|------|------|----------|------|
```

### 更新规则

- 每完成一项任务，在表格末尾追加一行，填写完成日期和任务简述。
- 备注为可选项，供用户记录主观感受（如"蘑菇比例差很多"、"AA 感觉找到感觉了"）。
- 完成日期格式：`YYYY-MM-DD`。
- 推进"当前位置"指针到下一个未完成任务。

### 读取进度

确定下一任务时，只看 `progress.md` 的已完成记录——不依赖日历，不估算"应该到哪了"，严格按 `exercise-plan.md` 的任务顺序往后推。

---

## 图片分析

当 `week-XX/` 目录下存在 `.png` 文件时，agent 可以：

- 对比临摹作品与参考原作，给出像素级具体反馈（如"第 3 行第 7 列的 AA 像素颜色偏深"）。
- 识别常见问题：锯齿不均匀、调色板超限、光源方向不一致、比例失调等。
- 反馈语言：具体指出位置和问题，给出可操作的修改建议，不泛泛而谈。

图片命名约定（供 agent 解析）：

```
week-01/day-02-heart.png          # 第1周第2天，临摹心形
week-06/original-character-v2.png # 原创角色第2版
```

---

## 工作约定

- **语言**：所有输出（反馈、进度更新、追加内容）均使用中文。
- **资源引用**：补充教程或练习时，优先引用 Lospec、saint11.art、Slynyrd 的一手资料，注明链接。
- **不替用户做决定**：临摹对象、调色板、画布尺寸如果有多种合理选项，列出选项让用户选，不擅自决定。
- **弹性节奏**：用户工作繁忙，不强调"今天必须完成"，只关注"按顺序完成下一项"。
